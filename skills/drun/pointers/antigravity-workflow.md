---
name: drun
description: Use the globally installed drun skill
---

Load and follow the installed `drun` skill. Inspect `.drun/spec.drun` and use
`xdrun --list` to discover repository automation before suggesting commands.
