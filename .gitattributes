# Ensure consistent line endings across platforms
# All text files should use LF line endings

# Default behavior: normalize line endings to LF on checkin, 
# but don't convert on checkout (leave as LF)
* text=auto eol=lf

# Explicitly specify that .drun files should always use LF
*.drun text eol=lf

# Go source files
*.go text eol=lf

# YAML files
*.yml text eol=lf
*.yaml text eol=lf

# Markdown files
*.md text eol=lf

# Shell scripts
*.sh text eol=lf

# Ensure Windows batch files use CRLF (if any exist)
*.bat text eol=crlf
*.cmd text eol=crlf

# Binary files should not be modified
*.exe binary
*.dll binary
*.so binary
*.dylib binary
